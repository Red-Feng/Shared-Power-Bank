<view class="indexContainer">
  <view class="map">
    <map 
      id="myMap" 
      markers="{{a}}" 
      latitude="{{i}}" 
      longitude="{{j}}" 
      style="width:100%;height:100%" 
      show-location 
      scale="{{k}}"
    >
    </map>
  </view>
  
  <!-- 左侧功能浮窗 -->
  <view class="leftFloatPanel">
    <view class="floatItem" bindtap="{{f}}">
      <image class="floatIcon" src="/static/images/搜索.png" mode=""></image>
      <text class="floatText">搜索</text>
    </view>
    <view class="floatItem" bindtap="{{g}}">
      <image class="floatIcon" src="/static/images/聊天.png" mode=""></image>
      <text class="floatText">客服中心</text>
    </view>
    <view class="floatItem" bindtap="{{h}}">
      <image class="floatIcon" src="/static/images/地图定位.png" mode=""></image>
      <text class="floatText">定位</text>
    </view>
  </view>
  
  <view class="indexBottom">
    <view class="bottomItem" bindtap="{{b}}">
      <image class="itemImg" src="/static/images/商超便利.png" mode=""></image>
      <text class="itemText">附近门店</text>
    </view>
    <view class="bottomCenter" bindtap="{{c}}">
      <image class="centerImg" src="/static/images/扫码.png" mode=""></image>
      扫码充电
    </view>
    <view class="bottomItem" bindtap="{{d}}">
      <image class="itemImg" src="/static/images/云管家.png" mode=""></image>
      <text class="itemText">个人中心</text>
    </view>
  </view>
  <view wx:if="{{e}}" class="maskContainer">
    <view class="content">正在扫码，请不要关闭小程序</view>
    <mask class="mask" bindclick="{{l}}" u-i="10b2a639-0" bind:__l="__l"></mask>
  </view>
</view>