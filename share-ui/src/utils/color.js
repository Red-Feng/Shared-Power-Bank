// 图表配色工具，供 dashboard 使用

// 默认的一组图表颜色（可以根据需要调整）
const DEFAULT_CHART_COLORS = [
  '#4D8DFF',
  '#00B8A0',
  '#FFB057',
  '#FE6E6E',
  '#8EBEF3',
  '#9891DF'
]

/**
 * 获取图表颜色列表
 * @returns {string[]}
 */
export function getChartListColor() {
  return DEFAULT_CHART_COLORS
}


